
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ВремяНачалаЗамера = ОценкаПроизводительности.НачатьЗамерВремени();
	ТЗ = ОбменHTTPСевис.ПолучитьМарки(Строка(ОБЪЕКТ.ССЫЛКА.УникальныйИдентификатор()), "hs", "");
	Если ТЗ <> Неопределено Тогда
		Марки.Очистить();
		Для каждого стр из ТЗ Цикл
			стр1 = Марки.Добавить();
			стр1.Марка = стр.Марка;   
			Если стр.ИдНоменклатура <> "" Тогда
				стр1.Номенклатура = справочники.Номенклатура.ПолучитьСсылку(Новый УникальныйИдентификатор(стр.ИдНоменклатура));
			КонецЕсли;
		КонецЦикла;     
	КонецЕсли;  
	 ОценкаПроизводительности.ЗакончитьЗамерВремени("Получение марок через http-сервис",ВремяНачалаЗамера,Марки.Количество(), "Вес по табличной части ""Марки""");
КонецПроцедуры
